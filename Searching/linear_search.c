//WAP to find an element from array by taking the input from user. Using Linear Search.

#include<stdio.h>
main()
{
    int n,element,i,count=0;
    printf("Enter the number of element in an array: /n");
    scanf("%d",&n);
    
}